fn factorial(n: &int): int {

}

fn main(): int {
  let mut x: int = 0;
  let y: &int = &mut x;

  if (true) {
    let x: int = 0;
    factorial(x);
  }
  
  factorial(x);

  let mut x: int = 3;

  factorial(y);

  return x;
}

Ownership:
1. max one owner.

references:
1. unlimited immutable references
2. max 1 mutable reference
3. no mixing mutable references

lifecycle:

